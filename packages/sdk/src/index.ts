// Curated public API for @qkb/sdk.
//
// Modules are extracted incrementally from packages/web/src/lib. Each
// extraction lands as its own commit and exposes its surface here.
//
// Until extraction is complete, this file deliberately exports nothing —
// importing from "@qkb/sdk" will yield an empty module. Use the per-module
// subpaths during the migration window.

export {};
