# qkb-v5-stub.zkey — pump deferred (2.2 GB)

The stub zkey is too large for git (gitignored as `*.zkey` in arch-circuits).

## Absolute path on this host
```
/data/Develop/qkb-wt-v5/arch-circuits/packages/circuits/ceremony/v5-stub/qkb-v5-stub.zkey
```

## Integrity
sha256: 0643c7c4ace35e8d142add140e11fbcdfd6998185989c8d08de6889ddd7ace95

## For browser proving
Cannot ship the zkey via git. Either:
1. Reference absolute path in dev (as above) for local browser tests.
2. Upload to R2 / CDN before §9.4 acceptance gate (post-real-ceremony work).

## NOT for production
Single-contributor stub. Replaced by real Phase 2 ceremony output (§11) before mainnet.
