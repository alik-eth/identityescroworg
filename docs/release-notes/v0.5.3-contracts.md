# V5.3 contracts release — `rotationNewWallet` 160-bit range-check (F2)

**Package:** `@qkb/contracts`, `@qkb/contracts-sdk`, `@qkb/sdk`
**Version:** `0.5.2-pre` → `0.5.3-pre`
**Branch:** `feat/v5_3-contracts` (commit `1b260d8`)
**Spec:** `docs/superpowers/specs/2026-05-03-v5_3-oid-anchor-amendment.md` (founder-approved v0.1)
**Predecessor release:** `docs/release-notes/v0.5.2-contracts.md` (V5.2 keccak-on-chain amendment, `v0.5.2-pre-ceremony`)

V5.3 is an **in-place amendment to the V5.2 contract** (NOT a new contract). The V5.2 amendment design picks up F2 (rotationNewWallet 160-bit range-check) on the contract side; F1 (subject-serial OID-anchor) and F3 (walletSecret↔msgSender doc) are circuits-eng's track. Public-signal layout is **frozen at V5.2's 22 signals** — this release does not touch ABI shape, selectors, storage layout, or downstream SDK fixtures.

## TL;DR for downstream consumers

| Consumer surface | Breaking change? | Action required |
|---|---|---|
| `IQKBRegistry` view ABI | **No** — selectors byte-identical V5.2 → V5.3 | None. |
| `register(...)` / `rotateWallet(...)` write ABI | **No** — function selectors unchanged (public-signal tuple shape unchanged) | None. SDK + integrators keep `qkbRegistryV5_2Abi` from `@qkb/sdk`. |
| Witness builder / circuit | **Yes** — circuit gains F1 (private input `subjectSerialOidOffsetInTbs`) + F2 in-circuit `Num2Bits(160)` + F3 doc | Use `buildWitnessV5_3` from `@qkb/sdk` (web-eng T5). Public-output layout still 22 — no calldata change. |
| Constructor signature | **No** — `(IGroth16VerifierV5_2, address, bytes32, bytes32)` unchanged | None. |
| Storage layout (off-chain indexers) | **No** — slots 0..6 byte-identical V5.2 → V5.3 | None. |
| Contract address on Sepolia / mainnet | **Yes for the verifier; no for the registry source** — the registry source is `QKBRegistryV5_2.sol` (still); a fresh deploy is required because the registry's `groth16Verifier` is `immutable` and must point at the new V5.3 ceremonied verifier | Re-run `script/DeployV5_2.s.sol` with `GROTH16_VERIFIER_ADDR` set to the V5.3 ceremony output. Update `fixtures/contracts/sepolia.json` post-deploy. |
| Custom error selectors | **No** — `InvalidNewWallet()` semantics generalize without selector change | None. |

## Why V5.3 (contract-side)

V5.2's contract review surfaced three findings; F2 is the contract-side one. The V5.2 `rotateWallet()` flow contains a silent-truncation surface at the public-signal unpack:

```solidity
address newWallet = address(uint160(sig.rotationNewWallet));
```

`sig.rotationNewWallet` is a public-signal slot (`uint256`, BN254 field-element-shaped). Without a range-check, a malicious prover can submit `sig.rotationNewWallet = (high_bits << 160) | uint160(prover_addr)` — the cast yields `prover_addr`, the downstream `newWallet != msg.sender` check passes (prover prank as `prover_addr`), and the proof's slot 17 carries an arbitrary 254-bit field element that the proof commits to but the contract never validates against the cast result.

This is a **defense-in-depth gap** rather than an immediate exploit (the attacker still needs to satisfy the Groth16 pairing equation against the published verifier, and the V5.3 in-circuit `Num2Bits(160)` constraint will close the same hole at the circuit layer once circuits-eng's track lands). But the load-bearing-on-each-other discipline (gate at both circuit and contract) means either layer can be refactored without re-introducing the truncation surface.

## Contract-side change (one diff)

In `QKBRegistryV5_2.rotateWallet()`, between the existing `WrongMode` mode gate and the Groth16 verify call:

```solidity
// V5.3 F2: rotationNewWallet 160-bit range check.
if (sig.rotationNewWallet != uint256(uint160(sig.rotationNewWallet))) {
    revert InvalidNewWallet();
}
```

Insertion is at the **earliest fail-fast position** so malformed proofs don't pay the ~400K Groth16 verifier cost. The check uses the existing `InvalidNewWallet()` error declaration from V5.2 — semantics generalize from V5.2's "newWallet == 0 || == oldWallet || != msg.sender" to "+ bits 160..255 nonzero" without selector change.

## What about `register()`?

**Not added** — and intentionally so. The V5.2 `register()` flow already enforces an equivalent bound via the existing `WrongRegisterModeNoOp` gate:

```solidity
if (sig.rotationNewWallet != uint256(uint160(msg.sender))) {
    revert WrongRegisterModeNoOp();
}
```

`uint256(uint160(msg.sender))` is bounded by `2^160 - 1` by construction (any uint160 cast produces a value ≤ uint160.max). For this gate to PASS, `sig.rotationNewWallet` MUST equal that value, hence MUST be < 2^160. F2 in `register()` would be **structurally redundant** — the existing gate already enforces the 160-bit bound as a side effect of the equality check.

Adding F2 to `register()` for spec-uniformity over structural redundancy was considered and rejected:
- ~60 gas extra on the high-volume register path (vs zero benefit; the gate would never fire / never catch anything)
- Future readers see two "same" gates and assume something subtle is going on
- If `WrongRegisterModeNoOp` is ever refactored away, the resulting bug ("register-mode rotateNewWallet unbounded") would be in plain sight + caught by its own test coverage anyway, NOT silenced by a second gate

The contract-side F2 scope is therefore **rotateWallet-only**. Spec §F2.2's "add to both flows" guidance is being amended to reflect this in the v0.2 spec refresh.

## ABI changes

### Function selectors

**No changes.** Verified via `forge inspect QKBRegistryV5_2 methodIdentifiers` against the V5.2 baseline. Same 15 selectors, byte-identical:

| Function | V5.2 → V5.3 selector |
|---|---|
| `register(...)` | `0x9ab660c7` (unchanged) |
| `rotateWallet(...)` | `0x9849ff37` (unchanged) |
| `isVerified(address)` | `0xb9209e33` (unchanged) |
| `nullifierOf(address)` | `0x61e0a22e` (unchanged) |
| ... all other view / admin / getter selectors | byte-identical |

**Implication:** all SDK consumers (read or write) continue to use `qkbRegistryV5_2Abi`. No re-bind required.

### Public-signal layout

**No changes.** V5.3 keeps the V5.2 frozen layout exactly (22 signals, slots 0..21). F2's contract-side check operates on slot 17 (`rotationNewWallet`) without altering its position or shape. F1 + F3 (circuits-eng's track) add private witness inputs but no new public outputs.

### Custom errors

**No new selectors.** F2 reuses the existing `InvalidNewWallet()` (`0xbdf7f17f`) declared in V5.2:

| Error | V5.2 selector | V5.3 selector | Notes |
|---|---|---|---|
| `InvalidNewWallet()` | `0xbdf7f17f` | `0xbdf7f17f` | **Semantics generalized** from "newWallet == 0 \|\| == oldWallet \|\| != msg.sender" to "+ bits 160..255 nonzero". Selector unchanged ⇒ revert classifiers do not need updating. |

All other 28 error selectors byte-identical V5.2 → V5.3.

## Storage layout

**Identical V5.2 → V5.3** — verified via `forge inspect QKBRegistryV5_2 storageLayout` diff. F2 adds no new state variables; the gate is calldata-driven + revert-only. Same 7 slots V5/V5.1/V5.2/V5.3:

| Slot | Name | Type |
|---|---|---|
| 0 | `admin` | `address` |
| 1 | `trustedListRoot` | `bytes32` |
| 2 | `policyRoot` | `bytes32` |
| 3 | `nullifierOf` | `mapping(address => bytes32)` |
| 4 | `identityCommitments` | `mapping(bytes32 => bytes32)` |
| 5 | `identityWallets` | `mapping(bytes32 => address)` |
| 6 | `usedCtx` | `mapping(bytes32 => mapping(bytes32 => bool))` |

## Constructor signature

**No changes.** `QKBRegistryV5_2.constructor(IGroth16VerifierV5_2, address, bytes32, bytes32)` is byte-identical V5.2 → V5.3. `script/DeployV5_2.s.sol` is reusable for the V5.3 deploy with no edits — only the `GROTH16_VERIFIER_ADDR` env value changes (point at the V5.3 ceremonied verifier instead of the V5.2 one).

## Gas measurements

Per-call F2 cost on `rotateWallet()` (when fired), measured via `forge snapshot` diff of `packages/contracts/snapshots/gas-snapshot.txt`:

| Test path (rotateWallet) | V5.2 gas | V5.3 gas | Δ |
|---|---|---|---|
| `test_rotateWallet_v52_revertsCommitmentMismatch` | 1,813,858 | 1,813,922 | +64 |
| `test_rotateWallet_v52_revertsInvalidNewWallet_whenSenderNotNewWallet` | 1,814,352 | 1,814,416 | +64 |
| `test_rotateWallet_v52_revertsInvalidRotationAuth_whenSigByWrongKey` | 1,820,788 | 1,820,852 | +64 |
| `test_rotateWallet_v52_revertsUnknownIdentity_whenFingerprintNotClaimed` | 31,676 | 31,740 | +64 |
| `test_rotateWallet_v52_revertsWrongMode_whenMode0` | 1,808,980 | 1,808,984 | +4 (jumptable noise) |

**True F2 per-call cost: ~60 gas** (rotateWallet paths that reach F2 see a uniform +64 gas; the WrongMode path which reverts BEFORE F2 sees only +4 gas, indicating the +60 is genuinely the F2 gate firing; the +4 elsewhere is bytecode-position-shift compiler noise).

Other small gas deltas across non-rotate tests (~14-100 gas across constructor, register, admin, setter paths) are bytecode-position shifts from adding ~400 bytes of new revert path. **Compiler noise — not real per-call cost.** `RealTupleGasSnapshotV5_2` register-path measurements unchanged (F2 gate not on the register code path).

V5 spec §3 acceptance gate (revised at commit `def6270` from 600K → 2.5M) still holds with comfortable margin.

## What stayed the same

- **`IQKBRegistry` interface** — selectors and return types byte-identical V5/V5.1/V5.2/V5.3.
- **Storage layout** — 7 slots, byte-identical to V5.2.
- **Constructor signature** — `(verifier, admin, trustRoot, policyRoot)` parity-equivalent.
- **`register()` flow** — F2 not added (structurally redundant per §"What about register()?"); V5.2 register-path gas measurements unchanged.
- **Public-signal layout** — 22 signals, frozen per V5.2 §"Public-signal layout".
- **All non-`InvalidNewWallet` custom error selectors** — byte-identical.
- **`rotateWallet()` rotation auth model** — ECDSA sig from `oldWallet`'s privkey over the rotation payload (chainid + registry addr + fingerprint + newWallet); F2 fires before the auth check.
- **`script/DeployV5_2.s.sol`** — reusable for V5.3 deploy with same env layout; only `GROTH16_VERIFIER_ADDR` changes.
- **First-claim nullifier semantics** — V5.1 invariant 4 (`nullifierOf` write-once on first-claim, migrates atomically on `rotateWallet`) preserved verbatim.

## Migration guidance

### For SDK consumers (web, integrators)

1. Bump dependency: `@qkb/sdk@0.5.3-pre`.
2. **No ABI re-bind needed.** All selectors byte-identical V5.2 → V5.3. Continue using `qkbRegistryV5_2Abi`.
3. **No revert-classifier change.** `InvalidNewWallet`'s selector is preserved; the new high-bits-set trigger condition reverts with the same selector existing classifiers already handle.
4. **For witness builders**: pick up V5.3 witness-builder additions (F1's `subjectSerialOidOffsetInTbs` private input) when web-eng T5 lands. Public-output layout still 22 — no calldata change.

### For deployer-tooling

1. Use `script/DeployV5_2.s.sol` (unchanged) for the V5.3 deploy.
2. Point `GROTH16_VERIFIER_ADDR` at the V5.3 ceremonied verifier (post-Phase-B V5.3 multi-contributor ceremony at `ceremony/v5_3/`).
3. Same env layout otherwise: `PRIVATE_KEY`, `ADMIN_ADDRESS`, `INITIAL_TRUST_ROOT`, `INITIAL_POLICY_ROOT`, `MINT_DEADLINE`, optional `CHAIN_LABEL`.

### For holders

V5.2 → V5.3 is a **fresh deploy**, not an upgrade. The registry contract itself (`QKBRegistryV5_2.sol`) is non-upgradeable; the F2 gate is in-place source amendment; the bytecode that rolls is the new bytecode of the V5.3 deploy + a new V5.3-ceremonied `groth16Verifier` immutable. Existing V5.2 holders re-register against the new V5.3 registry via the standard fresh-QES flow.

The two registries (V5.2 and V5.3) can coexist on-chain during the transition window; downstream consumers point at one or the other via deployment-fixture configuration (`fixtures/contracts/{sepolia,mainnet}.json`).

## Acceptance

- `forge build`: clean (`via_ir`, `optimizer_runs=200`).
- `forge test`: **412 passed / 0 failed / 1 skipped / 413 total** (was 410 in V5.2 baseline; +2 V5.3 F2 tests — `test_rotateWallet_v53_revertsInvalidNewWallet_whenRotationNewWalletExceeds160Bits` + `test_rotateWallet_v53_succeeds_atUint160Boundary_2pow160Minus1`; 1 env-gated skip = `test_deploy_base_sepolia_fork`).
- `forge snapshot`: refreshed at `packages/contracts/snapshots/gas-snapshot.txt` (commit `1b260d8`).
- Cross-package integrity: SDK ABI surface unchanged (no re-pump needed); `pnpm -F @qkb/sdk test` continues to pass against `qkbRegistryV5_2Abi` from V5.2 baseline.

## Outstanding before mainnet

- **V5.3 multi-contributor Phase-B ceremony** — produces the V5.3 `groth16Verifier` (the V5.3 circuit's `Num2Bits(160)` constraint changes the R1CS, requiring a new Phase-2 ceremony; pot22 is reusable, no need for pot23).
- **V5.3 stub ceremony script** — circuits-eng T3 produces `Groth16VerifierV5_3Stub.sol` for real-pairing gas measurement against the V5.3 vkey.
- **V5.3 RealTupleGasSnapshot integration test** — sibling to `RealTupleGasSnapshotV5_2.t.sol` against the V5.3 stub. Will gain a rotate-path fixture so the V5.3 happy-path can be tested as a true round-trip (the V5.3 unit-suite F2 boundary test uses a downstream-revert surrogate because the V5.2 unit fixture's `holder` address is keccak-derived without a known privkey).
- **Sepolia E2E gate** (orchestration §9.4) — full register flow against real V5.3 ceremonied verifier on Base Sepolia.
- **web-eng T5 completion** — V5.3 witness builder (F1 private input + F2/F3 no-op) + SDK fixtures.

## References

- V5.2 contracts release: `docs/release-notes/v0.5.2-contracts.md`
- **V5.3 amendment design (founder-approved v0.1):** `docs/superpowers/specs/2026-05-03-v5_3-oid-anchor-amendment.md`
- V5.3 orchestration plan: `docs/superpowers/plans/2026-05-03-v5_3-orchestration.md`
- V5.2 amendment spec: `docs/superpowers/specs/2026-05-01-keccak-on-chain-amendment.md`
- V5.2 contract-side review (PASS-WITH-NOTES): `docs/superpowers/specs/2026-05-01-keccak-on-chain-contract-review.md`
- Gas-acceptance revision (600K → 2.5M): commit `def6270`
