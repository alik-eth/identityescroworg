<!-- This page is a render of `BRAND.md` at the repository root.
     The include directive below pulls the source at build time so
     there is exactly one canonical source of truth for the brand
     split — no duplication, no drift between the repo's BRAND.md
     and the doc-site rendering. Edits go to `/BRAND.md`. -->
<!--@include: ../BRAND.md-->
