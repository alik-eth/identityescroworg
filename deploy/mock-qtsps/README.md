# `deploy/mock-qtsps`

Docker-compose harness for three mock QTSP escrow agents used by the Phase 2
integration E2E. Owned by `qie-eng` per `docs/superpowers/plans/2026-04-17-qie-qie.md`
Task 14. This directory is a scaffold placeholder — the real
`docker-compose.yml` lands with that task.

Consumed by: `packages/web` Playwright `qie-full` project (per `qie-web` plan
Task 9) and ad-hoc lead E2E dry-run (per orchestration §8 L1).
