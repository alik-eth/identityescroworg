# Circuits ceremony → uploadthing artifact hosting.
# Obtain these at https://uploadthing.com ; free tier (2 GB) covers Phase 1.
# NEVER commit a real .env file — only .env.example.
UPLOADTHING_TOKEN=
UPLOADTHING_SECRET=

# Web runtime — public uploadthing URLs produced by circuits upload step.
# These are PUBLIC hosted files; safe to hard-code or distribute.
QKB_PROVER_WASM_URL_RSA=
QKB_PROVER_ZKEY_URL_RSA=
QKB_PROVER_WASM_URL_ECDSA=
QKB_PROVER_ZKEY_URL_ECDSA=
